<%= form_for(dumpsite) do |f| %>
  <% if dumpsite.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dumpsite.errors.count, "error") %> prohibited this dumpsite from being saved:</h2>

      <ul>
      <% dumpsite.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :address %>
    <%= f.text_field :address %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
