<%= form_for(work_order_status) do |f| %>
  <% if work_order_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(work_order_status.errors.count, "error") %> prohibited this work_order_status from being saved:</h2>

      <ul>
      <% work_order_status.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
